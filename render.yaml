version: 1
services:
  - type: web
    name: invidious
    env: ubuntu
    buildCommand: "apt-get update && apt-get install -y $(cat requirements.txt)"
    startCommand: "./invidious -b 0.0.0.0 -p $PORT"
    plan: free
    healthCheckPath: /
